define(['bus', 'underscore', './page-controller',
        './components/online-store-data', 'console-logger'],
    function (bus, _, PageController) {

        var controller = new PageController();

    }
);